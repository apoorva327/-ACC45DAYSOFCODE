#include<stdio.h>
int main() {
  double f, s, swap;
  printf("Enter first number: ");
  scanf("%lf", &f);
  printf("Enter second number: ");
  scanf("%lf", &s);
  swap = f;
  f = s;
  s = swap;
  printf("\n now first number = %f\n", f);
  printf("and second number = %f", s);
  return 0;
}
