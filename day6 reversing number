#include <stdio.h>

int main() {

  int n, b = 0, r;

  printf("Enter number: ");
  scanf("%d", &n);

  while (n != 0) {
    r = n % 10;
    b = b * 10 + r;
    n /= 10;
  }

  printf("by reversing we get = %d", b);

  return 0;
